# Bugs — Fix these BEFORE working on any PLAN.md tasks

(none)

- **Dashboard layout needs reorganization**: The dashboard is currently a single column. Reorganize into a two-column layout on desktop: LEFT column has "My Groups" summary widget and bracket list. RIGHT column has live ESPN scores and recent results feed. On mobile, stack them (right column content on top since scores are time-sensitive, left column below). Use CSS grid or flexbox, keep it responsive.
