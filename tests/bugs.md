# Bugs — Fix these BEFORE working on any PLAN.md tasks

- **CRITICAL - Cannot select teams in bracket**: On /bracket/1, clicking on team names does nothing — picks cannot be made despite the bracket being unlocked. The click handler is either not attached, not firing, or the bracket is incorrectly being treated as locked/read-only. Test by clicking any team in the Round of 64 and verifying the team advances to the next round.

- **Bracket visuals are significantly worse than the original app**: The bracket at /bracket/1 is missing key visual elements that the original MarchMadness app (~/TestProjects/MarchMadness) has. Specific issues: (1) No connector lines between rounds — games in later rounds should have lines/brackets connecting them to their feeder games. Look at how the original app does this in src/components/bracket/RegionBracket.tsx. (2) Games in later rounds are NOT vertically centered between their two feeder games from the previous round — a Round of 32 game should be vertically centered between the two R64 games that feed into it, Sweet 16 centered between its two R32 feeders, etc. This is the fundamental visual property of a bracket. (3) Study the original app's bracket CSS and layout approach at ~/TestProjects/MarchMadness/src/components/bracket/ and replicate the visual quality. The bracket should look like a real tournament bracket, not a flat list of games.
