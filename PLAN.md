# March Madness Picker — Development Plan

Agent picks the top incomplete task. When all tasks are done, study competitor sites and propose small free improvements.

## Tasks
- [x] Build comprehensive test suite: The app has 59 routes/pages and ZERO tests. Add three layers of testing: (1) Update verify.sh to check ALL pages and API routes — every page.tsx should return 200, every API route should return valid JSON (401 for auth-required is OK). (2) Add unit tests for core logic in src/lib/ using a test runner (vitest or jest) — test scoring.ts (scorePicks, upset bonuses, tiebreaker), bracket-utils.ts (cascade clearing, getTeamsForGame), autofill.ts (smart/chalk/random), and grading.ts. (3) Add API integration tests that login as testbot (source ~/.config/testapp-creds.env), create a bracket, save picks, join a group, and verify leaderboard scores. Write tests in tests/ directory. Run with `npm test`. Add a `test` script to package.json.
- [x] Research and decide on tech stack: study pros/cons of Next.js vs Remix vs plain React+Express, SQLite vs PostgreSQL vs JSON files, MUI vs Tailwind vs shadcn. Write findings to specs/tech-decisions.md. Consider: free hosting, simplicity, bundle size, developer experience. Then scaffold the chosen stack.
- [x] Implement user auth: register/login with username+password, bcrypt hashing, JWT cookies. First user becomes admin.
- [x] Implement tournament and bracket data model: DB schema for users, tournaments, picks, groups. See specs/bracket.md and specs/groups.md.
- [x] Build the interactive bracket UI: 68-team bracket with First Four, click-to-pick, cascade clearing. See specs/bracket.md.
- [x] Build groups system: create/join via invite link, custom scoring settings, group leaderboards. See specs/groups.md.
- [x] Build scoring and leaderboard: per-round points, upset bonuses, tiebreaker, round-by-round breakdown. See specs/scoring.md.
- [x] Add live scores from ESPN public API and auto-resolve results.
- [x] Add autofill options (Smart/Chalk/Random) as dropdown with descriptions. Only fill empty slots.
- [x] Add bracket export (PNG with readable colors).
- [x] Add admin panel: create tournaments, import bracket data, sync ESPN results.
- [x] Add "What-If" simulator: bracket view where clicks set hypothetical results, live leaderboard sidebar.
- [x] Add bracket comparison overlay: one bracket showing multiple users' picks with colored indicators.
- [x] Add pick agreement stats to comparison view: show fraction of selected brackets that agree on each game (e.g., "3/4") and highlight unanimous picks vs split picks with color coding.
- [x] Add bracket completion progress: show a progress bar on dashboard and group pages indicating how many of 63 picks are filled per bracket, with a "Complete" badge when all picks are in. Helps friends see who still needs to finish before lock time.
- [x] Add lock countdown timer: show a live countdown to tournament lock_time on the dashboard and bracket edit page. Display "Locked" badge after lock time passes. Disable pick changes after lock. Helps friends know exactly how much time remains to finalize brackets.
- [x] Add bracket stats page: show most popular champion picks with bar chart, biggest upset pick, most chalk vs most contrarian bracket. Available after lock time. Linked from dashboard nav.
- [x] Add "Who Picked Whom" page: for a selected group, show every matchup with which members picked which team, counts, and percentages. Helps friends see consensus and contrarian picks at a glance. Linked from group page and dashboard nav.
- [x] Add user profile page: show a user's brackets with per-round score breakdown, groups they belong to, and join date. Clickable from leaderboards and group pages. Helps friends see each other's performance at a glance.
- [x] Add group chat: simple real-time messaging within each group so friends can trash-talk and discuss picks. Chat panel on the group detail page with polling. DB-backed messages table.
- [x] Add scoring breakdown dialog: clicking a user's score on the leaderboard opens a dialog showing every resolved pick — round, team picked, correct/wrong, base points, upset bonus. Helps friends see exactly which picks earned or lost points. Reuse existing scorePicks logic, extend it to return per-game detail.
- [x] Add persistent navigation bar: create a shared Navbar component shown on all authenticated pages with links to Dashboard, Groups, Stats, and profile. Currently each page has ad-hoc navigation buttons which is inconsistent. Include user greeting and logout button. Replaces per-page nav buttons.
- [x] Add pick reminder banner: show an urgent banner on the dashboard when a user has incomplete brackets and lock time is within 24 hours. Color-coded (warning within 24h, error within 1h). Links to the bracket edit page. Helps friends avoid missing the deadline. show an urgent banner on the dashboard when a user has incomplete brackets and lock time is within 24 hours. Color-coded (warning within 24h, error within 1h). Links to the bracket edit page. Helps friends avoid missing the deadline.
- [x] Add results-updated banner: when admin syncs ESPN results, track the timestamp in the tournaments table. Show a dismissible banner on the dashboard telling users "Results updated at [time]" so friends know the leaderboard is fresh. Dismissal persists via localStorage until the next update. Inspired by MarchMadness app's ResultsBanner.
- [x] Add champion pick and busted indicator to leaderboard: show each bracket's champion pick on the leaderboard row. Display a 💀 "Busted" badge if that team has been eliminated from the tournament (lost a game in results). Helps friends quickly see who picked whom to win it all and whose bracket is dead.
- [x] Add pick distribution overlay: after lock time, show what percentage of all brackets picked each team in every matchup. API endpoint aggregates all brackets for a tournament and returns per-game pick percentages. Displayed as small percentage labels on each team in the bracket GameCard. Helps friends see consensus vs contrarian picks at a glance.
- [x] Add team logos: display NCAA team logos from ESPN CDN next to team names in bracket GameCards, comparison view, simulator, who-picked view, and champion display. Uses free ESPN CDN URLs (a.espncdn.com). Makes the bracket visually richer and easier to scan at a glance.
- [x] Add max possible points to leaderboard: show each bracket's maximum possible score (current points + points from remaining alive picks) on the leaderboard. Helps friends see who's still in contention vs mathematically eliminated. Classic ESPN Tournament Challenge feature.
- [x] Add "eliminated from contention" indicator: on the group leaderboard, show a 🚫 badge next to brackets whose max possible score is less than the current leader's score. This tells friends at a glance whose bracket is mathematically dead — they can't win the pool even if every remaining pick is correct. Classic ESPN Tournament Challenge feature. Also show "Best Possible Finish" (the highest rank a bracket could achieve) in the leaderboard.
- [x] Add Final Four picks preview to leaderboard: show each bracket's 4 Final Four team picks as small team logos on the leaderboard row (in addition to the existing champion column). Lets friends quickly compare everyone's Final Four without clicking into each bracket. Classic ESPN Tournament Challenge feature. API returns the 4 Final Four picks per bracket; UI shows them as compact team logos with seed numbers.
- [x] Add bracket duplicate: on the dashboard, add a "Duplicate" button next to each bracket that copies all picks and tiebreaker to a new bracket with "(Copy)" appended to the name. Lets friends quickly create bracket variations without re-picking 63 games from scratch. Classic ESPN Tournament Challenge feature. Also add a delete button on the dashboard for bracket management. Only available before lock time.
- [x] Add dark mode toggle: add a dark/light mode toggle to the Navbar that persists via localStorage. Apply dark mode styles across all pages using Tailwind's dark mode class strategy. Many users browse brackets late at night during games — dark mode reduces eye strain and looks great. Free, no external dependencies.
- [x] Add bracket mini-summary on dashboard: show each bracket's champion pick and Final Four picks as small team logos on the dashboard bracket list, so users can see their key picks at a glance without clicking into each bracket. Uses existing bracket picks data and TeamLogo component. Makes the dashboard more informative and visually appealing.
- [x] Add tiebreaker input to bracket edit page: show a "Predicted Championship Total Score" input field on the bracket page so users can enter their tiebreaker value. The API already supports saving tiebreaker, and the leaderboard already displays it, but there's no UI to enter it. Essential for breaking ties in March Madness pools. Save tiebreaker alongside picks.
- [x] Add mobile-responsive navbar: convert the Navbar to use a hamburger menu on small screens. Currently nav links overflow on mobile, making the app unusable on phones. Friends check brackets on their phones during games — this is essential UX. Use Tailwind responsive classes, no external dependencies.
- [x] Add tournament round progress widget: show a compact widget on the dashboard displaying the current tournament round, games completed vs total in that round, and an overall tournament progress bar (e.g., "Sweet 16 — 4/8 games complete · 36/63 overall"). Helps friends see at a glance where the tournament stands. Analyze results data to determine current round. Classic ESPN Tournament Challenge feature.
- [x] Add global tournament leaderboard page: show all brackets across the entire tournament ranked by score (not just within a group). Accessible from Navbar. Uses default scoring settings. Shows rank, username, bracket name, round-by-round scores, total, champion pick, busted/eliminated indicators, max possible, and best possible finish. Clickable bracket names link to read-only bracket view. Classic ESPN Tournament Challenge "Overall Leaderboard" feature. Helps friends see how they rank against everyone in the tournament.
- [x] Add sortable leaderboard columns with correct picks count: allow clicking column headers to sort the leaderboard by any metric (total, each round, max possible, correct picks). Add a "Correct" column showing total correct picks out of resolved games (e.g., "28/40"). Classic ESPN Tournament Challenge feature. Friends love sorting by different metrics to find bragging rights ("I had the best Sweet 16!"). Applies to both group and global leaderboards.
- [x] Add round recap page: for a selected group, show a round-by-round recap with per-round MVP (who scored the most that round), biggest upset picks that hit, most busted picks, and leaderboard movement (who moved up/down between rounds). Linked from group page. Helps friends see highlights and bragging rights after each round. Classic ESPN Tournament Challenge "Round Recap" feature. API endpoint computes per-round stats from existing scoring data.
- [x] Add bracket health indicator on dashboard: for each bracket card, show a compact health summary — correct picks (✅), alive picks (🟢 future picks where team is still alive), and busted picks (💀 future picks where team is eliminated). Color-coded green/yellow/red. Computed from bracket picks vs tournament results and eliminated teams. Gives friends an instant visual of how their bracket is doing without navigating to the leaderboard. Classic ESPN "Bracket Health" feature.
- [x] Add bracket grade: assign a letter grade (A+ through F) to each bracket based on correct pick percentage and percentile ranking from the global leaderboard. Show the grade prominently on the dashboard bracket card and profile page. Computed from existing scoring data — no new API needed beyond a lightweight grade endpoint. Fun social comparison feature inspired by ESPN's "How's Your Bracket?" grade. FREE, no external dependencies.
- [x] Add bracket share text button: add a "Share" button on the bracket page that copies a formatted text summary of the bracket's key picks (Champion, Final Four, Elite Eight picks, notable upset picks) to the clipboard. Friends can paste this into group chats, texts, or social media. More useful than just sharing a URL because it works for people without accounts. Also include the bracket URL. Inspired by ESPN's share feature. FREE, no external dependencies.
- [x] Add recent results feed with pick impact: show a compact section on the dashboard displaying the most recently resolved games (from results_data) with how each of the user's brackets was affected — ✅ correct or ❌ wrong, points earned, and seed matchup. Sorted by most recent. Helps friends quickly see "what just happened and how did it affect my bracket?" during the tournament. Classic ESPN Tournament Challenge "Recent Results" feature. Uses existing results_data and bracket picks — no new API needed, just a lightweight endpoint. FREE, no external dependencies.
- [x] Add leaderboard search/filter: add a search input to the GroupLeaderboard component that filters rows by username or bracket name. Useful for pools with many participants — friends can quickly find themselves or each other without scrolling. Classic ESPN Tournament Challenge feature. Applies to both group and global leaderboards. FREE, no external dependencies.
- [x] Add head-to-head bracket comparison: on the group leaderboard, allow selecting two brackets to see a focused 1v1 comparison — games where each was right/wrong, overall record (e.g., "15-12"), who has the better remaining bracket, and key disagreements. Opens in a dialog. Classic ESPN Tournament Challenge "Head-to-Head" feature. Drives friendly competition between pool members. FREE, no external dependencies.
- [x] Add group activity feed: show a chronological feed of group events on the group detail page — member joins, bracket added to group, bracket completed (all 63 picks), bracket updated. Stored in a new `group_activity` DB table. Displayed as a compact timeline in a new "Activity" tab on the group page. Helps friends see who's active and creates social urgency ("everyone else has finished their bracket!"). Classic ESPN Tournament Challenge feature. FREE, no external dependencies.
- [x] Add group standings history chart: show a pure-SVG line chart on the group page visualizing how each bracket's rank changed after each round. X-axis = rounds (R64 through Championship), Y-axis = rank (inverted so #1 is at top). Each bracket is a colored line. Hover shows bracket name and rank. Computed from existing per-round scoring data — the API endpoint calculates cumulative scores after each round and derives rankings. Displayed in a new "📈 Standings" tab on the group detail page. Friends love watching the "race" unfold — who was leading after the first weekend, who surged in the Elite Eight, etc. Classic ESPN Tournament Challenge "Standings History" feature. FREE, pure SVG, no external charting library needed.
- [x] Add group invite QR code: on the group detail page, add a "Show QR Code" button next to the invite link that displays a QR code for the invite URL. At March Madness watch parties, the host can show the QR code on their phone/screen and friends scan to instantly join — much easier than copying/pasting URLs. Pure client-side SVG QR code generation using a minimal QR encoding algorithm (no external library). Displayed in a modal/popover. FREE, no external dependencies.
- [x] Add group pool description: let the group creator add a text description to the group — pool rules, entry fee, payout structure, house rules, etc. Add a `description` column to the groups table, a text area in the group creation form and settings tab, and display it on the group detail page header. Essential for real March Madness pools where friends need to know the rules. FREE, no external dependencies.
- [x] Add bracket pick streak indicator: show a 🔥 hot streak or ❄️ cold streak badge on the dashboard bracket cards and group leaderboard rows. Computed from the most recent resolved games — count consecutive correct or incorrect picks. "🔥 5" means 5 correct in a row, "❄️ 3" means 3 wrong in a row. Fun social comparison metric during the tournament. API endpoint computes streaks from existing bracket picks vs results data. FREE, no external dependencies.
- [x] Add "My Groups" summary widget on dashboard: show a compact card for each group the user belongs to, displaying their best rank, score, group leader name/score, and member count. Lets friends see "I'm #3 in Office Pool, #1 in Family Pool" at a glance without navigating to each group. Links to group detail page. Classic ESPN Tournament Challenge "My Groups" widget. Uses existing group leaderboard API. FREE, no external dependencies.
- [x] Add bracket achievements/badges: award fun badges based on bracket picks and results — "Perfect Round" (all picks correct in a round), "Oracle" (3+ upset picks correct), "Chalk Master" (all favorites in R64), "Chaos Agent" (most upsets picked in group), "Diamond in the Rough" (12+ seed to Sweet 16 correct), "Champion Caller" (correct champion), "Hot Hand" (10+ correct streak). Computed from existing scoring data. API endpoint returns achievements per bracket. Displayed on dashboard bracket cards and profile page. Fun social comparison — friends love comparing badges. FREE, no external dependencies.
- [x] Add scoring rules help page: create a `/rules` page that explains how the bracket works (68 teams, First Four, 4 regions, Final Four), how scoring works (points per round, upset bonuses, tiebreaker), and how groups/pools work (custom scoring, invite links). Dynamically shows the default scoring values from constants. Linked from Navbar. Essential for friends who are new to March Madness pools — "how does scoring work?" is the #1 question in every pool. FREE, no external dependencies.
- [x] Add "Who's Left?" page: show all remaining (non-eliminated) teams in the tournament organized by region, with seed, team logo, and how far each team has advanced. Eliminated teams shown grayed out with strikethrough. Helps friends quickly see the state of the tournament field — "which 1-seeds are still alive?", "any Cinderellas left?". Linked from Navbar. Classic ESPN Tournament Challenge "Who's Left?" feature. Uses existing bracket_data and results_data — no new API needed beyond a lightweight endpoint. FREE, no external dependencies.
- [x] Add upset tracker page: show all upsets (lower seed beat higher seed) that have occurred in the tournament, organized by round. For each upset, show the winning team (seed, logo), the losing team (seed, logo), the round, and how many brackets correctly predicted it (with percentage). Highlight "biggest upset" and "most predicted upset." Linked from Navbar. Classic March Madness feature — friends love discussing "did anyone pick that 15 over 2?!" Uses existing bracket_data, results_data, and bracket picks. FREE, no external dependencies.
- [x] Add mobile-friendly bracket view: on small screens (<768px), replace the full 1200px bracket layout with a region-tab view. Show tabs for each region + Final Four. Each tab shows that region's games stacked vertically, round by round. Uses existing GameCard/FinalFour components. The bracket currently requires 1200px min-width and horizontal scrolling on mobile — friends check brackets on their phones during games and this makes it unusable. Detect screen width with a custom hook and conditionally render mobile vs desktop layout. FREE, no external dependencies.
- [x] Add bracket print stylesheet: add `@media print` CSS rules to globals.css that hide the navbar, buttons, and non-essential UI, and optimize the bracket layout for printing on paper. Add a "🖨️ Print" button next to the existing Export PNG button on the bracket page. At March Madness watch parties, printed brackets are a tradition — friends fill them out on paper and compare. The PNG export works for digital sharing but browser print produces cleaner paper output with proper page sizing. Uses native `window.print()`. FREE, no external dependencies.
- [x] Add bracket pick undo/redo: add undo (⌘Z / Ctrl+Z) and redo (⌘⇧Z / Ctrl+⇧Z) support to the bracket edit page, plus visible ↩️ Undo / ↪️ Redo buttons. When filling out 63 games, accidentally clicking the wrong team triggers cascade-clearing of downstream picks — undo lets users instantly recover. Maintain a pick history stack in the useBracketPicks hook. Classic UX improvement that prevents frustration during bracket filling. FREE, no external dependencies.
- [x] Add pool payout calculator: let the group creator set a buy-in amount and payout structure (number of paid places + percentage split) in group settings. Display calculated payouts on the group leaderboard based on member count. Shows "1st: $140, 2nd: $40, 3rd: $20" etc. Essential for real March Madness pools where friends need to know the prize structure. Add `buy_in` and `payout_structure` columns to groups table. FREE, no external dependencies.
- [x] Add CSV export for group leaderboard: add a "📥 Export CSV" button on the group detail page that downloads the current leaderboard as a CSV file (rank, username, bracket name, round scores, total, max possible, champion pick, correct picks). Pool commissioners running real money pools need to export standings for record-keeping, sharing with people who don't have accounts, and tracking payouts. Pure client-side CSV generation from existing leaderboard data — no new API needed. Also add to global leaderboard page. FREE, no external dependencies.
- [x] Add bracket rename: on the dashboard, add a "✏️ Rename" button next to each bracket (before lock time) that lets users change the bracket name inline. Currently the only way to change a bracket name is to delete and recreate it. Add a PATCH endpoint to `/api/brackets/[id]` that updates the name. Simple inline editing with an input field that appears on click. FREE, no external dependencies.
- [x] Add bracket insights panel: on the bracket view page (after lock time), show a collapsible "📊 Bracket Insights" panel with computed stats about the user's bracket — chalk percentage (how many higher seeds picked), number of upsets picked per round, most contrarian pick (lowest pick distribution %), most popular pick, champion popularity ranking. Uses existing pick distribution data from the `/api/brackets/distribution` endpoint. Pure client-side computation, no new API needed. Helps friends understand and compare their bracket strategy at a glance. Inspired by ESPN's "Bracket Insights" feature. FREE, no external dependencies.
- [x] Add seed matchup win rates tooltip: when hovering over a game in the bracket editor (before lock time), show a tooltip with historical NCAA tournament seed matchup win rates (e.g., "1 vs 16: Higher seed wins 99.4%"). Hardcoded data from well-known historical stats. Helps friends who are new to March Madness make informed picks — "should I pick this 12 over the 5?" becomes easier when you see "12 seeds beat 5 seeds 35.7% of the time." Displayed as a small info icon or hover tooltip on each GameCard. Pure client-side, no API needed. FREE, no external dependencies.
- [x] Add bracket auto-save: automatically save bracket picks after 2 seconds of inactivity instead of requiring a manual save button click. Show a subtle "✓ Auto-saved at [time]" indicator that replaces the current "Unsaved" warning. Keep the manual Save button as a fallback. Prevents the common frustration of forgetting to save and losing picks when navigating away. Pure client-side change to the existing useBracketPicks hook — add a debounced useEffect that triggers save when dirty. No new API, no new DB, no new components. FREE, no external dependencies.
- [x] Add password change: let users change their password from their profile page. Show a "Change Password" section when viewing your own profile with current password, new password, and confirm fields. API endpoint verifies current password with bcrypt, hashes new password, updates DB. Essential account security feature — currently users have no way to change their password after registration. FREE, no external dependencies.
- [x] Add admin user management: add a "Users" section to the admin panel showing all registered users with username, join date, bracket count, and admin status. Admin can reset a user's password (generates a temporary password shown once) and toggle admin status. Essential for friend groups — someone WILL forget their password and there's no email recovery. Also lets the pool commissioner promote co-admins. API endpoint at `/api/admin/users`. FREE, no external dependencies.
- [x] Add in-app notification center: add a 🔔 bell icon to the Navbar with an unread count badge. Track key events as notifications — results synced by admin, new group chat messages, bracket lock time approaching (1h warning), new member joined your group, and round completion. Store in a `notifications` DB table (user_id, type, message, link, read, created_at). Bell click opens a dropdown showing recent notifications with timestamps, clickable links, and "Mark all read" button. Notifications are created server-side when events happen (ESPN sync, group join, chat message) and by a lightweight check on dashboard load for time-based alerts. Essential for a social bracket app — friends miss important updates because they don't check the dashboard. ESPN Tournament Challenge pushes notifications; this is the in-app equivalent. FREE, no external dependencies.
- [x] Add bracket reset button: on the bracket edit page (before lock time), add a "🔄 Reset All Picks" button that clears all 63 picks and the tiebreaker, letting users start fresh. Currently the only way to start over is to delete the bracket and create a new one, which loses the bracket name and any group assignments. Uses the existing `bulkSetPicks` function in useBracketPicks hook. Confirm with a dialog before clearing. FREE, no external dependencies.
- [x] Add "My Picks Tonight" widget: show a compact widget on the dashboard displaying today's/tonight's games (from results_data or bracket_data matchups that are currently in progress or scheduled) and for each game, show which team each of the user's brackets picked to advance. During March Madness watch parties, the #1 question is "who did I pick in this game?" — this answers it instantly without clicking into each bracket. Uses existing bracket_data matchups and bracket picks. Determine "today's games" by matching results_data games that don't have a winner yet (in-progress or upcoming in the current round). If no live games, show the next round's matchups. Displayed as a compact card on the dashboard above the bracket list. FREE, no external dependencies.
- [x] Add tournament results bracket page: create a `/results` page showing the actual tournament results as a filled-in bracket. Displays the real outcomes (winners) for each completed game, with team logos and seeds, organized in the standard bracket layout. Unplayed games show the matchup if known or "TBD". Uses existing bracket_data (for matchups/seeds) and results_data (for winners) from the tournament. Reuses existing RegionBracket/FinalFour/MobileBracket components in read-only mode with results as picks. The most-viewed page on ESPN during the tournament — friends constantly check "what actually happened?" without wanting to see it overlaid on their own picks. Linked from Navbar. FREE, no external dependencies.
- [x] Add "My Picks vs Results" overlay on results page: add a bracket selector dropdown on the `/results` page that lets authenticated users overlay their own picks on the results bracket. When a bracket is selected, each resolved game shows a ✅ or ❌ indicator based on whether the user's pick matches the actual result. Show a summary bar with correct/wrong/pending counts. Helps friends instantly see "how did my bracket do?" in context of the actual results without switching between pages. Uses existing bracket picks data and results data — fetches user's brackets from `/api/brackets` and overlays picks on the results view. Pure client-side overlay, no new API needed. Classic ESPN Tournament Challenge "My Bracket vs Results" feature. FREE, no external dependencies.
- [x] Add buy-in payment tracking for group pools: let the group admin mark which members have paid their buy-in. Add a `paid` boolean column to the `group_brackets` table. On the group detail page Settings tab, show a payment tracker with each member's name and a toggle to mark them as paid/unpaid. Show a "💰 Paid" or "⚠️ Unpaid" badge next to each member on the leaderboard. Show a summary "12/15 members paid ($240 of $300 collected)" on the group page. Essential for real money March Madness pools — the commissioner always needs to chase people for payment. API endpoint PATCH `/api/groups/[id]/payment` to toggle payment status (admin only). FREE, no external dependencies.
- [x] Add "Games That Matter" widget: on the dashboard, show a compact section highlighting upcoming/in-progress games that are most impactful to the user's bracket standings. For each of the user's groups, identify games where the user's pick differs from the picks of brackets ranked above them — these are "swing games" where a correct pick could move them up. Show the game, the user's pick, and which group rivals picked the other team. Helps friends focus on the games that actually affect their pool standing during watch parties. Computed from existing bracket picks and group leaderboard data. API endpoint `/api/brackets/games-that-matter` takes a user's brackets and returns relevant upcoming games with rival pick info. FREE, no external dependencies.
- [x] Add leaderboard rank movement indicators: show ↑/↓ arrows next to each bracket's rank on the group and global leaderboards, indicating how their rank changed compared to the previous round's standings. E.g., "↑3" (green) means moved up 3 spots, "↓2" (red) means dropped 2. Computed by scoring brackets using only results through the previous round to get previous rankings, then comparing to current rankings. Classic ESPN Tournament Challenge feature that creates excitement and conversation among friends. Add `previousRank` to LeaderboardEntry type. Pure server-side computation from existing data. FREE, no external dependencies.
- [x] Add keyboard navigation for bracket filling: when editing a bracket, allow arrow keys (↑/↓) to move between games and number keys (1/2) or Enter to pick the top/bottom team. Show a subtle highlight on the currently focused game. Highlight the focused GameCard with a ring. Pressing Tab moves to the next unfilled game. Makes filling out 63 games much faster than clicking each one — essential UX for the core bracket experience. Pure client-side keyboard event handling in the bracket page. FREE, no external dependencies.
- [x] Add "Copy Standings" button to group page: add a 📋 button next to the CSV export on the group detail page that copies a formatted, human-readable text summary of the current standings to the clipboard. Format: group name, round info, then ranked list with emoji medals (🥇🥈🥉), bracket name, score, champion pick, and streak. Designed for pasting into group texts, iMessage, Slack, or Discord — pool commissioners constantly share standings updates during the tournament. Pure client-side text generation from existing leaderboard data. Also add to global leaderboard. FREE, no external dependencies.
- [x] Add live scores ticker to bracket page: show the existing LiveScores component on the bracket view page (above the bracket) so users can see real-time game scores while viewing or editing their bracket. During March Madness, users constantly check their bracket while watching games — having live scores right on the bracket page saves switching between pages. Reuses existing LiveScores component and ESPN scores API. One-line addition to the bracket page. FREE, no external dependencies.
- [x] Add bracket team search/highlight: add a search input on the bracket page that highlights a specific team's path through the bracket. When the user types a team name, all GameCards containing that team get a colored ring/border highlight, and the view scrolls to the first match. During March Madness, friends constantly ask "where is [team] in the bracket?" — this makes it instant to find any team across all 4 regions and 6 rounds. Pure client-side filtering using existing bracket data and picks. Pass a `highlightTeam` string prop to RegionBracket/FinalFour/GameCard components. Add a search input above the bracket (desktop) or in the mobile tab bar. ESPN Tournament Challenge has this feature. FREE, no external dependencies.
- [x] Add mini bracket preview on leaderboard hover: when hovering over a row on the group or global leaderboard, show a popover with a mini bracket visualization — the 4 Final Four picks, 2 championship game teams, and the champion, displayed as a compact bracket with team logos and seeds. Lets friends quickly preview anyone's key picks without clicking into the full bracket. The MarchMadness reference app has this feature. Uses existing bracket picks data already returned by the leaderboard API (finalFourPicks, champion). Pure client-side popover component, no new API needed. FREE, no external dependencies.
- [x] Add bracket similarity matrix: on the group detail page, add a "🔀 Similarity" tab showing a matrix of how similar each pair of brackets in the group is. For each pair, show the percentage of matching picks (out of games both brackets have filled). Highlight the most similar pair ("Bracket Twins 👯") and most different pair ("Polar Opposites 🔄"). Computed client-side from existing leaderboard bracket picks data — API endpoint `/api/groups/[id]/similarity` returns all bracket picks for the group. Drives fun conversation in friend groups ("You and I only agree on 40% of picks!"). FREE, no external dependencies.
- [x] Add scoring presets for group creation: when creating a group, show preset buttons (Standard, Upset Heavy, Progressive, Flat) above the manual scoring inputs. Each preset fills in the scoring values with one click. Standard = default (1,2,4,8,16,32 / no upset bonus). Upset Heavy = same base points but with upset bonuses (1,2,3,5,8,13). Progressive = seed-based multiplier feel (2,3,5,8,13,21). Flat = equal points per round (10,10,10,10,10,10). Makes group creation faster and helps new users who don't know what scoring values to use. Also add presets to the group settings tab for existing groups. Classic CBS/Yahoo bracket pool feature. FREE, no external dependencies.
- [x] Add bracket pick list view: on the bracket page, add a toggle to switch between the visual bracket layout and a compact list/table view showing all picks organized by round. Each row shows: round name, team picked (with seed and logo), opponent team (with seed), and result indicator (✅ correct / ❌ wrong / ⏳ pending). Much easier to scan on mobile and great for quick reference during watch parties — "did I pick Duke in the Sweet 16?" is answered instantly. Uses existing bracket picks, regions, and results data already loaded on the bracket page. Pure client-side component, no new API needed. ESPN Tournament Challenge has this as "My Picks" list view. FREE, no external dependencies.
- [x] Add bracket emoji reactions on group leaderboard: let group members add quick emoji reactions (🔥 🗑️ 💀 👑 🤡) to each other's brackets on the group leaderboard. Lightweight social interaction that drives engagement — friends can flame a bad bracket or crown a hot one without typing in chat. Store in a `bracket_reactions` table (group_id, bracket_id, user_id, emoji, created_at) with a unique constraint on (group_id, bracket_id, user_id, emoji) so each user can add each emoji once per bracket. API endpoint GET/POST/DELETE at `/api/groups/[id]/reactions`. UI shows small emoji counts below each leaderboard row; clicking an emoji toggles it. Clicking your own reaction removes it. Inspired by Slack/Discord reactions. FREE, no external dependencies.
- [x] Add rich link previews with Open Graph meta tags: add dynamic OG meta tags to key pages (bracket view, group detail, leaderboard, results) so when friends share URLs in iMessage, Slack, Discord, or WhatsApp, the link shows a rich card with the page title, description, and a basketball-themed preview image. Use Next.js Metadata API with `generateMetadata` for dynamic pages (bracket shows bracket name + champion pick, group shows group name + member count). Generate a simple SVG-based OG image using Next.js `ImageResponse` at `/api/og` endpoint. Also add a proper favicon (basketball SVG). The #1 way friends share bracket links is pasting URLs in group chats — rich previews make these links look professional and clickable instead of bare URLs. Classic web best practice. FREE, no external dependencies.
- [x] Add watch party TV mode: create a `/party` page optimized for displaying on a TV or projector during March Madness watch parties. Shows live scores in large readable text, group standings (selectable group dropdown), and recent game results with bracket impact — all auto-refreshing every 30 seconds. Large fonts, high contrast, minimal chrome. Full-screen toggle button. Designed for viewing from across a room at a watch party. Linked from Navbar. Reuses existing LiveScores, GroupLeaderboard data, and results data. FREE, no external dependencies.
- [x] Add admin database backup: add a "💾 Download Backup" button on the admin panel that downloads the SQLite database file as a .db attachment. Essential for a local-only app with no cloud backup — losing the database means losing all users, brackets, groups, and scores permanently. API endpoint GET `/api/admin/backup` streams the database file with proper Content-Disposition header. Admin-only access. One button, one endpoint. FREE, no external dependencies.
- [x] Add group member bracket completion tracker: on the group detail page, before lock time, show a "📋 Bracket Status" section listing all group members with their bracket completion status — ✅ complete (63/63 picks), ⚠️ incomplete (with pick count like "42/63"), or ❌ no bracket submitted to this group yet. Add a "📲 Copy Reminder" button that copies a pre-written nudge message to clipboard: "Hey! [N] people in [Group Name] still need to finish their brackets before [lock time]. Don't miss the deadline! [invite link]". The #1 pain point in every March Madness pool is getting everyone to finish before tip-off — the commissioner constantly chases people. This makes it visible and actionable. API endpoint GET `/api/groups/[id]/member-status` returns all members with their bracket completion info. Displayed on the group detail page Brackets tab or as a standalone section. FREE, no external dependencies.
- [x] Add Second Chance Bracket: after the first weekend, let users create a "second chance" bracket starting from the current tournament state. Only remaining (non-eliminated) teams are available. The bracket covers only the remaining rounds (e.g., Sweet 16 onward). When a friend's champion gets knocked out early, they lose interest — Second Chance gives them a reason to stay engaged. Create a `/second-chance` page that reads tournament results, determines remaining teams and the current round, and presents a mini-bracket of remaining matchups. Save as a regular bracket with a `is_second_chance` flag. Reuse existing bracket components (GameCard, RegionBracket, FinalFour, MobileBracket) in a filtered mode. Add a "🔄 Second Chance" button on the dashboard when the tournament has progressed past R64. Scored separately on the leaderboard (shown as "2nd Chance" column or separate tab). Classic ESPN Tournament Challenge "Second Chance Bracket" feature. FREE, no external dependencies.
- [x] Add trash talk generator: add a "🗣️ Trash Talk" button on the group and global leaderboard that generates a contextual, funny trash talk message based on current standings — roasts the leader, burns last place, calls out busted champions, highlights hot/cold streaks, and notes eliminated brackets. Uses randomized template strings with dynamic data. Copies to clipboard for pasting into group chats, iMessage, Slack, Discord. Drives social engagement and friendly competition during the tournament. Pure client-side, no API needed. FREE, no external dependencies.
- [x] Add bracket scoring summary bar: on the bracket view page, show a compact always-visible stats bar below the bracket title showing the user's current score, correct/wrong/pending pick counts, max possible points, and their rank in each group this bracket belongs to. During the tournament, users constantly switch between bracket and leaderboard pages to check their standing — this eliminates that need. Fetch scoring data from existing `/api/groups/[id]/leaderboard` endpoints for groups this bracket belongs to. Compute correct/wrong/pending from picks vs results_data. Show as a compact horizontal bar with pill-shaped stats. Only visible after lock time (when results exist). Reuses existing scoring logic from `src/lib/scoring.ts`. Classic ESPN Tournament Challenge "My Bracket Score" bar. FREE, no external dependencies.
- [x] Enhance landing page: redirect logged-in users to /dashboard automatically. For non-logged-in visitors, show an engaging landing page with feature highlights (bracket filling, group pools, live scoring, leaderboard), a basketball-themed hero section, and public tournament status (current round, games completed) if a tournament exists. The current landing page is just a title and two buttons — this is the first impression of the app and should sell the experience. Server-side auth check for redirect, client component for the feature showcase. FREE, no external dependencies.
- [x] Complete dark mode coverage: the global CSS overrides in globals.css handle common gray-scale classes but miss many colored backgrounds (bg-blue-50, bg-purple-50/100, bg-orange-100, bg-amber-50/100/200, bg-indigo-50) and their hover/text variants. Add missing global dark mode overrides for all colored utility classes used across the app. Also add dark mode overrides for the ResultsBanner, LockCountdown, and other components that use colored backgrounds without explicit dark: classes. Efficient approach: extend globals.css with missing color overrides rather than editing 60+ individual files. FREE, no external dependencies.
- [x] Add PWA support (Add to Home Screen): add a web app manifest with app name, basketball-themed icons (192px and 512px PNG generated from existing SVG), theme color, and `display: standalone`. Add manifest link and `theme-color` meta tag to the layout. Also add an `apple-touch-icon` for iOS. This lets users "Add to Home Screen" on their phones for a native-app-like experience — during March Madness, friends check brackets dozens of times a day and having it as a home screen icon instead of a browser bookmark is a huge UX win. Basic installability requires only the manifest and icons — no service worker needed for the install prompt. FREE, no external dependencies.
- [x] Add command palette (⌘K / Ctrl+K): add a global keyboard shortcut that opens a search/command palette modal. Users can type to quickly navigate to any page (Dashboard, Groups, Stats, Rules, etc.), search for teams (highlights team path in bracket), or jump to a specific group or bracket by name. Results update as you type with fuzzy matching. Arrow keys to navigate, Enter to select, Escape to close. Displayed as a centered modal with a search input and categorized results (Pages, Groups, Brackets, Teams). Modern UX pattern (like VS Code, Slack, Linear) that makes the 60+ page app instantly navigable. Pure client-side component using existing data from `/api/auth/me`, `/api/brackets`, `/api/groups`. FREE, no external dependencies.
- [x] Add leave group and remove member: let users leave a group via a "Leave Group" button on the group detail page (with confirmation dialog). Also let group admins (creator) remove other members from the group. When leaving/removed, all of that user's brackets are unassigned from the group. API endpoint DELETE `/api/groups/[id]/members` accepts a `user_id` param — if the requesting user is the group creator they can remove anyone, otherwise they can only remove themselves (leave). Essential for real pools — someone joins the wrong group, or needs to leave a pool. Currently there's no way to leave a group after joining. FREE, no external dependencies.
- [x] Add group prop bets: let group members create yes/no prediction questions ("Will a 16 seed make the Sweet 16?", "Will the final be a rematch?") that other members vote on. Creates daily engagement beyond bracket picks — friends love side bets during March Madness. Store in a `group_predictions` table (id, group_id, creator_id, question, resolved, correct_answer, created_at). Votes in `prediction_votes` table (prediction_id, user_id, vote boolean). Display in a "🎲 Props" tab on the group detail page. Group creator can resolve predictions (yes/no). Show vote counts and who voted what after resolution. API endpoints: GET/POST `/api/groups/[id]/predictions`, PATCH to resolve. FREE, no external dependencies.
- [x] Add navbar score badge: show the user's best group rank and score as a compact badge in the Navbar (e.g., "🏆 #3 · 142pts"). Fetches from `/api/groups/my-summary` which already returns per-group rank/score data. During March Madness, the #1 thing friends check is "what's my rank?" — having it always visible in the nav eliminates constant navigation to the leaderboard. Only shown after lock time when scores exist. Pure client-side addition to the existing Navbar component using existing API. FREE, no external dependencies.
- [x] Add "Add to Calendar" button for lock deadline: add a 📅 button next to the LockCountdown timer on the dashboard and bracket edit page that downloads an .ics calendar file for the bracket lock deadline. The .ics file creates a calendar event titled "March Madness Brackets Lock" with a 1-hour reminder alarm. Friends constantly miss the bracket deadline — a calendar reminder on their phone ensures they finish in time. Pure client-side .ics file generation using a Blob download. No external dependencies. FREE.
- [x] Add public bracket sharing via link: generate a unique share token per bracket so users can share a read-only view of their bracket with anyone — no login required. Add a `share_token` column to the brackets table (random UUID, generated on first share). Public page at `/bracket/share/[token]` renders the bracket in read-only mode with team logos, seeds, and results overlay. Add a "🔗 Share Link" button on the bracket page that generates/copies the public URL. Essential for social sharing — friends who haven't signed up can view your bracket from a link in iMessage, Slack, or social media. ESPN Tournament Challenge has this. FREE, no external dependencies.
- [x] Add keyboard shortcuts help dialog: pressing `?` anywhere (except when typing in an input) opens a modal listing all available keyboard shortcuts — ⌘K/Ctrl+K for command palette, ⌘Z/⌘⇧Z for undo/redo on bracket page, arrow keys and 1/2 for bracket game navigation, Tab for next unfilled game, Escape to close. Standard UX pattern (GitHub, Gmail, Slack). Users currently have no way to discover the keyboard shortcuts we've built. Small `KeyboardShortcutsHelp` component rendered in layout, triggered by global `?` keydown listener. FREE, no external dependencies.
- [x] Add custom 404 page and global error boundary: create a `not-found.tsx` at the app root with a basketball-themed 404 page ("Air Ball! Page not found") with a link back to the dashboard, and a `global-error.tsx` / `error.tsx` error boundary that catches runtime errors and shows a friendly "Something went wrong" message with a retry button. Currently the app shows Next.js default error pages which look broken and confuse users. Essential for any production app — friends sharing bracket links that get deleted or mistyped URLs see an ugly default page. Uses existing Tailwind dark mode classes. FREE, no external dependencies.
- [x] Add multi-bracket quick switcher: on the bracket page, add a compact dropdown next to the bracket name that lists all of the user's other brackets for the same tournament. Clicking a bracket in the dropdown navigates to that bracket's page instantly. Users with multiple brackets constantly go back to the dashboard just to switch — this saves a round trip. Fetch the user's bracket list from `/api/brackets` (already loaded on dashboard). Show bracket name and completion status. Only visible when the user has 2+ brackets. Pure client-side navigation, no new API needed. FREE, no external dependencies.
- [x] Add payment link to group pools: let the group creator add a payment URL (Venmo, PayPal, Zelle, Cash App, or any link) in group settings. Add a `payment_link` column to the groups table. Display a clickable "💸 Pay Now" button next to the "⚠️ Unpaid" badge on the leaderboard and in the payment tracker. The commissioner pastes their payment link once, and every unpaid member sees a one-click button to pay. The #1 friction in real money March Madness pools is collecting payment — this eliminates the back-and-forth of "what's your Venmo?" Essential companion to the existing buy-in and payment tracking features. FREE, no external dependencies.
- [x] Add group pinned announcement: let the group admin set a pinned announcement that appears as a banner at the top of the group detail page. Essential for real March Madness pools — commissioners need to communicate rules, payment deadlines, payout info, and house rules prominently. Add an `announcement` column to the groups table. Admin can set/clear it in the group settings tab. Displayed as a dismissible 📌 banner above the leaderboard. FREE, no external dependencies.
- [x] Add admin tournament deletion: add a "🗑️ Delete Tournament" button on the admin panel for each tournament. Cascade deletes all related data — brackets, group_brackets assignments, bracket_reactions, notifications referencing the tournament, and group_activity entries. Confirm with a dialog requiring the user to type the tournament name. Essential admin feature — currently there's no way to delete test tournaments or clean up mistakes. API endpoint DELETE `/api/tournaments/[id]` (admin only). FREE, no external dependencies.
- [x] Add admin manual results entry:
- [x] Add admin broadcast notification: let the admin send a custom notification to all users from the admin panel. A text input + "📢 Send to All" button. Creates a notification row for every user in the existing `notifications` table. Essential for pool commissioners who need to announce things like "Brackets lock in 2 hours!", "Payment due by Friday", or "Congrats to the winner!". Reuses existing NotificationBell component — no new UI needed on the receiving end. One API endpoint POST `/api/admin/broadcast`, one small form on the admin page. FREE, no external dependencies. on the admin panel tournament detail, add a "✏️ Manual Results" section with an interactive bracket where the admin can click teams to set game winners — same click-to-pick UX as the bracket editor but for entering actual results. Essential for testing the app without ESPN, running non-NCAA tournaments, and correcting sync errors. Reuse existing GameCard/RegionBracket/FinalFour components in a "results entry" mode. Save results via the existing PUT `/api/tournaments/[id]` endpoint (updating results_data). Show current results with ability to clear individual games. Much more intuitive than the current ESPN-only sync approach. FREE, no external dependencies.
- [x] Add group submissions lock toggle: let the group admin lock/unlock bracket submissions for their group independently of the tournament lock time. Add a `submissions_locked` column to the groups table. Show a 🔒/🔓 toggle button in the group settings tab. When submissions are locked, members cannot add or modify brackets in that group (API enforces this). Show a "Submissions locked by group admin" badge on the group page. Useful for commissioners who want to close their pool early or prevent last-minute changes. Inspired by the MarchMadness reference app. FREE, no external dependencies.
- [x] Add auto-fill incomplete brackets at lock time: when the admin syncs ESPN results (or saves manual results) and the tournament is past lock time, automatically fill any incomplete brackets with chalk picks (higher seed wins). This ensures every bracket has 63 picks even if the user forgot to finish. Show a count of auto-filled brackets in the sync response. Notify affected users that their bracket was auto-completed. The reference MarchMadness app has this feature. Uses existing `generateAutofill` from `src/lib/autofill.ts`. FREE, no external dependencies.
- [x] Add bracket wizard (step-by-step fill): add a "🧙 Wizard" button on the bracket edit page that opens a guided, one-matchup-at-a-time fill mode. Shows the current game number (e.g., "Game 12 of 63"), the two teams with seeds, logos, and historical seed matchup win rates, plus "Pick Top" / "Pick Bottom" buttons. Arrow navigation between games. Progress bar at top. Skips already-filled games (or lets user review them). Especially useful on mobile where the full bracket is overwhelming — casual fans in friend groups often give up filling out a bracket because it's too complex. ESPN and CBS both have wizard modes. Pure client-side component using existing bracket data, picks, and useBracketPicks hook. No new API needed. FREE, no external dependencies.
- [x] Add leaderboard round time machine: add a round selector dropdown to the group and global leaderboard that lets users view standings as they were after any completed round. Uses existing `filterResultsBeforeRound` to compute scores through each round. Shows the full leaderboard with ranks and scores as of that round. Friends love debating "I was winning after the first weekend!" — this proves it. Add an optional `?asOfRound=N` query param to the leaderboard API endpoints. Add a dropdown to the GroupLeaderboard component. Classic ESPN Tournament Challenge "Time Machine" feature. FREE, no external dependencies.
- [x] Add group deletion: let the group creator delete their group from the group settings tab. Cascade deletes all related data — group_brackets, group_members, group_messages, group_activity, bracket_reactions, predictions, prediction_votes. Confirm with a dialog. The reference MarchMadness app has this feature but our app is missing it — currently there's no way to delete a group. API endpoint DELETE `/api/groups/[id]` (creator only). Add a "🗑️ Delete Group" button to the group settings tab. FREE, no external dependencies.
- [x] Add group awards ceremony: on the group detail page, add a "🏅 Awards" tab showing fun awards computed from existing scoring data — "🏆 Pool Champion" (highest total), "🔮 Oracle" (most correct picks), "🎯 Best Round X" (highest scorer per round), "📈 Biggest Mover" (most rank improvement between consecutive rounds), "🔥 Hottest Streak" (longest correct pick streak), "💀 Most Busted" (most eliminated picks), "🎲 Biggest Gambler" (most upsets picked). Each award shows the winner's bracket name, username, and the stat value. Computed from existing leaderboard, scoring, and streak data — API endpoint GET `/api/groups/[id]/awards` returns all awards. Drives fun conversation and bragging rights among friends. Classic ESPN Tournament Challenge "Pool Awards" feature. FREE, no external dependencies.
- [x] Add new user onboarding checklist: show a compact checklist on the dashboard for users who haven't completed setup — "✅ Create account", "📋 Create a bracket", "🏀 Fill out all 63 picks", "🎯 Set tiebreaker score", "👥 Join a group". Each step links to the relevant page. Auto-detects completion from existing data (brackets, picks count, tiebreaker, group membership). Dismissible via localStorage once all steps are done or manually dismissed. The #1 reason friends drop off is not knowing what to do after signing up — this guides them through the full flow. Especially useful when the pool commissioner sends an invite link and the friend has never used a bracket app. Pure client-side component using existing API data already loaded on the dashboard. FREE, no external dependencies.
- [x] Add bracket luck score to leaderboard: compute a "luck score" for each bracket by comparing actual score to expected score based on pick distribution. For each resolved game where the bracket has a pick, expected points = (pick_popularity_% / 100) × points_that_game_awards. Luck = actual_score - expected_score. Positive = lucky (got contrarian picks right), negative = unlucky (popular picks went wrong). Show as a 🍀 column on group and global leaderboards with green/red coloring. Drives great conversation — "You're only winning because you got lucky!" Computed server-side using existing pick distribution data and scoring logic. Add `luckScore` to LeaderboardEntry type. FREE, no external dependencies.
- [x] Add bracket notes: let users add a personal text note/strategy description to their bracket (e.g., "Picked Gonzaga because of their guard play"). Add a `notes` TEXT column to the brackets table. Show a collapsible text area on the bracket edit page (before lock time) and a read-only display after lock. Visible on the public share page and profile bracket list. Save notes via the existing PATCH `/api/brackets/[id]` endpoint. Friends love explaining their reasoning — Yahoo and CBS bracket managers have this. FREE, no external dependencies.
- [x] Add "Team Path to Championship" view: on the bracket page, add a "🛤️ Team Path" button that opens a modal where the user selects a team and sees that team's full path through the bracket — every round from R64 to Championship, showing the opponent (or potential opponent) in each round, whether the game is completed (with result), in progress, or upcoming. Highlight opponents that are still alive vs eliminated. Shows seed matchup win rates for upcoming games. The #1 question during March Madness is "Who does [team] play next?" and "What's their path to the Final Four?" — this answers it instantly. Uses existing bracket_data (for matchups/structure), results_data (for completed games), and historical seed win rates. Pure client-side component, no new API needed. ESPN Tournament Challenge has "Team Path" as a core feature. FREE, no external dependencies.
- [x] Add Monte Carlo win probability simulation: on the What-If simulator page, add a Monte Carlo simulation panel that runs 1000 simulations of remaining tournament games using historical seed win rates. For each unresolved game, randomly determine a winner based on seed matchup probabilities. Score all brackets in each simulation and compute: win probability (% of sims where bracket finishes #1), average score, and average finish position. Runs in a Web Worker to avoid blocking the UI. Shows a sortable table with progress bar during simulation. Re-runs automatically when hypothetical results change (debounced 500ms). Uses existing SEED_WIN_RATES from seed-matchup-history.ts. The reference MarchMadness app has this feature. Drives great conversation — "You have a 23% chance of winning the pool!" Classic ESPN Tournament Challenge feature. FREE, no external dependencies.
- [x] Add automatic ESPN result syncing:
- [x] Add spoiler-free mode: add a 🙈 toggle to the Navbar that hides all tournament results, scores, leaderboard rankings, and recent results from the UI. When enabled: dashboard hides Recent Results, Live Scores, Games That Matter, My Picks Tonight results, and leaderboard scores; bracket page hides result indicators (✅/❌) and live scores ticker; group pages hide leaderboard scores and chat (which may contain spoilers). Persists via localStorage. During March Madness, friends in different time zones watch games on delay — seeing scores or results ruins the experience. A single global toggle that blanks out spoiler content with a "🙈 Spoiler-free mode" placeholder. Pure client-side using React context, no API changes needed. FREE, no external dependencies. create a non-admin `/api/espn/auto-sync` endpoint that fetches ESPN scores and resolves tournament results with a 60-second debounce (skips if results were updated less than 60s ago). Triggered automatically by the dashboard and bracket pages on load via a lightweight client-side hook. During March Madness games, friends constantly refresh to see updated leaderboards — currently the admin must manually click sync every time. Auto-sync removes this bottleneck so results flow in automatically. The reference MarchMadness app has this feature. Debounce prevents ESPN API abuse. Also triggers auto-fill of incomplete brackets at lock time and notifications. Reuses existing `fetchEspnScores`, `resolveResults`, `autoFillIncompleteBrackets`, and `notifyResultsSynced` functions. FREE, no external dependencies.
- [x] Add admin "Add User to Group" feature: on the admin panel, add a section where the admin can search for users by username, select a group, and add the user to that group with optional bracket assignment. The reference MarchMadness app has this feature. Essential for pool commissioners who need to manually add someone who can't figure out the invite link, or organize brackets into groups. API endpoints: search users (GET `/api/admin/users?q=query`), get user's brackets (GET `/api/admin/users/[id]/brackets`), add user to group (POST `/api/admin/groups/add-member`). UI: Autocomplete user search, group dropdown, bracket checkboxes, "Add to Group" button. FREE, no external dependencies.
- [x] Add bracket pick history / changelog: track when users change their bracket picks before lock time. Store pick snapshots in a `bracket_history` table (bracket_id, picks JSON, tiebreaker, changed_at). Create a snapshot each time picks are saved (via PUT /api/brackets/[id]/picks) if picks actually changed. Add a "📜 History" button on the bracket page (visible to bracket owner) that opens a dialog showing timestamped changes — what picks were added/changed between each snapshot. Useful for settling disputes in friend groups ("I picked Duke before the game started!") and for users to review their own decision-making process. API endpoint GET `/api/brackets/[id]/history` returns snapshots. Limit to 50 snapshots per bracket to prevent bloat. Only available before and shortly after lock time. FREE, no external dependencies.
- [x] Add bracket import from text: add an "📋 Import Picks" button on the bracket edit page that opens a dialog where users can paste a list of team names (one per line, or comma-separated). The importer fuzzy-matches team names against the tournament's team list, then fills in bracket picks by finding the earliest unfilled game where that team could play. Useful for friends who filled out a paper bracket at a March Madness watch party and want to enter it digitally, or for copying picks from another source. Shows a preview of matched teams before applying. Uses existing `bulkSetPicks` from useBracketPicks. Pure client-side fuzzy matching, no new API needed. FREE, no external dependencies.
- [x] Add "Use as Template" button on shared brackets: when viewing someone else's public shared bracket (at `/bracket/share/[token]`), add a "📋 Use as Template" button that creates a new bracket for the authenticated user with all picks pre-filled from the shared bracket. Friends often want to start from someone else's bracket and make tweaks rather than filling out 63 games from scratch. API endpoint POST `/api/brackets/from-template` accepts a share_token, looks up the source bracket's picks, creates a new bracket named "[Original Name] (Template)" with those picks copied. Only available before lock time. Redirects to the new bracket's edit page. FREE, no external dependencies.
- [x] Add self-service password recovery via recovery code: at registration, generate a random 12-character recovery code and display it once to the user with a prompt to save it. Store the bcrypt hash of the code in a `recovery_hash` column on the users table. Add a `/forgot-password` page where users enter their username and recovery code to set a new password. The #1 support request in any app without email is "I forgot my password" — currently only the admin can reset it. This lets users recover their own account without bothering the pool commissioner. Also show the recovery code on the profile page behind a "Reveal" button (re-authenticate with current password first). API endpoint POST `/api/auth/recover` verifies username + recovery code hash, then sets new password. FREE, no external dependencies.
- [x] Add loading skeleton screens: replace plain "Loading..." text on the dashboard, bracket page, group page, and leaderboard with animated skeleton placeholder UI (pulsing gray bars/boxes). Currently these pages show a blank screen with tiny "Loading..." text which looks broken. Use Tailwind's `animate-pulse` on gray placeholder divs matching the layout shape. Pure CSS, no external dependencies. Improves perceived performance and looks professional. FREE, no external dependencies.
- [x] Add group pool recap summary page: create a `/groups/[id]/pool-recap` page that shows a comprehensive, shareable end-of-pool summary — final standings with medals (🥇🥈🥉), all group awards, tournament champion, biggest upsets predicted, most popular picks, and a "📋 Copy Recap" button that generates a formatted text summary for pasting into group chats/iMessage/Slack. Different from the existing round recap (per-round) — this is the full pool summary on one page. Linked from the group detail page. Reuses existing leaderboard API, awards API, and stats data. The page every pool commissioner shares when the tournament ends. FREE, no external dependencies.
- [x] Add dynamic group standings OG image: replace the generic OG image for group pages with a dynamic image that shows actual leaderboard standings (top 5 entries with scores, champion picks, and medals). When friends share a group URL in iMessage, Slack, Discord, or WhatsApp, the link preview shows the actual standings instead of just "Group Name - X brackets." Uses Next.js ImageResponse API at `/api/groups/[id]/og`. Queries group brackets, scores them with group scoring settings, and renders a styled leaderboard image. Drives sharing and engagement — the link preview IS the content. FREE, no external dependencies.
- [x] Add rooting guide widget: show a "🏀 Who to Root For" section on the dashboard that analyzes all the user's brackets to show which team they need to win in each upcoming/in-progress game. For each unresolved game where the user has a pick, show the team name, how many of their brackets picked that team, and the total potential points at stake across all brackets and groups. Sort by impact (most points at stake first). During March Madness watch parties, the #1 question is "who should I root for in this game?" — this answers it instantly. Different from "Games That Matter" (which focuses on group rivals) — this is personal: "YOU need Duke because you picked them in 3 brackets worth up to 24 points." API endpoint GET `/api/brackets/rooting-guide` computes from user's brackets, group scoring settings, and tournament results. Displayed as a compact card on the dashboard. FREE, no external dependencies.
